<h1> Delete <%= @judge.judge_name %> </h1>

<p> Are you sure you want to delete this judge? This action cannot be undone. </p>
<%= button_to 'Delete', judge_path(@judge), method: :delete, data: { turbo_confirm: 'Are you sure you want to delete this judge?' } %> |
<%= link_to 'Cancel', judges_path %>
